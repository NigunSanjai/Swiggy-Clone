import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App.tsx';
import './index.css';

// Function to add the Google Maps API script dynamically

ReactDOM.createRoot(document.getElementById('root')!).render(<App />);
